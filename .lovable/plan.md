## Goal\nMake `MerchantBusinessKycFlow` the primary onboarding CTA on the merchant landing page (`MerchantBenefitsPage` inside `src/pages/MerchantDashboard.tsx`) for users without approved business KYC.\n\n## Changes\n\n### `src/pages/MerchantDashboard.tsx` — `MerchantBenefitsPage`\n1. **Add state**:\n   - `kycFlowOpen: boolean`\n   - `kycStatus: 'none' | 'pending' | 'approved' | 'rejected'`\n   - `rejectionReason: string | null`\n   - `loadingStatus: boolean`\n\n2. **Fetch status on mount** (and on auth change):\n   ```ts\n   supabase.from('merchants')\n     .select('business_kyc_status, business_kyc_rejection_reason')\n     .eq('user_id', session.user.id)\n     .maybeSingle()\n   ```\n   Map result → `kycStatus`. If no row → `'none'`.\n\n3. **Status pill** above the main CTA:\n   - `pending` → amber badge \