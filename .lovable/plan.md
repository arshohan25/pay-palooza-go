

## Reduce QR Logo Clear Zone & Use Semi-Gray Background

Two small changes in `src/lib/qrWithLogo.ts`:

1. **Reduce padding** from `size * 0.15` to `size * 0.075` (50% smaller)
2. **Change background color** from `#ffffff` to a semi-gray like `#f0f0f0`

