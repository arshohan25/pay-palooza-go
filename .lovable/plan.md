

## Add Spacing Between Corner Brackets and Viewfinder

The emerald corner brackets are touching the edges of the viewfinder. I'll add `-2` offset positioning to pull them outward, creating an 8px spacing gap between the brackets and the viewfinder edge.

**Change:** Update the corner bracket positioning from `top-0 left-0` to `-top-2 -left-2` (and similar for all corners) to add visual breathing room.

